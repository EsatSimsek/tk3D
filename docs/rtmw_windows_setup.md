# Legacy RTMW Windows Environment

The default 2D pose pipeline has moved from RTMW to ViTPose-Huge WholeBody.

Use `docs/vitpose_windows_setup.md` for the active 2D inference environment and commands.

RTMW3D remains a separate optional single-view 3D helper module and is not the default 2D pose estimator.
